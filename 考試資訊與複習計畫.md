# 財團法人中小企業信用保證基金 115年儲備職員甄選
## 資訊人員－程式設計 (B41105102)

---

## 重要時程

| 項目 | 時間 |
|------|------|
| 報名截止 | 115年5月27日(三) 17:00 |
| 筆試 | 115年6月13日(六) 14:00–15:30（預備 13:50） |
| 筆試結果 | 115年7月16日(四) 14:00 |
| 口試資料上傳截止 | 115年7月20日(一) 17:00 |
| 口試 | 115年7月26日(日) |
| 錄取名單 | 115年8月3日(一) 14:00 |

考區：**僅設台北（雙北）**  
報名網址：https://svc.tabf.org.tw/115smeg01  
報名費：800元

---

## 職缺資訊

- 職等：7職等7級
- 月薪：**60,000餘元**（獎金、福利另計）
- 錄取名額：20名（進口試名額40名）
- 工作地點：台北為主
- 工作內容：本基金核心業務資訊系統均為**自行開發**

---

## 必要報考資格（均須具備）

1. 大學以上學歷
2. 資訊系統開發經驗 **2年以上**
3. 同時具備以下技能：
   - 熟悉 Web Form (ASP.NET)、物件導向程式設計、MS SQL Server
   - 熟悉網頁技術：HTML、JS、CSS
   - 擅長 Microsoft Visual Studio for C#
   - 具 .NET 8（含）以上開發經驗

---

## 筆試科目與題型

**綜合科目（100%），考試時間90分鐘**

| 科目 | 說明 |
|------|------|
| 程式設計 | 以 .NET、SQL 語言為主 |
| 系統分析、資料結構、資料庫應用 | |
| Microsoft Visual Studio for C# | |
| 物件導向程式設計 | |
| MS SQL Server 資料庫應用及設計 | |

**題型：**
- 選擇題：**20%**
- 非選擇題：**80%**（含程式設計、**除錯**，**限以C#手寫**）

---

## 成績計算

- 第一試（筆試）：占總成績 **50%**
- 第二試（口試）：占總成績 **50%**
- 口試未達70分 → 不予錄取

---

## 複習優先順序

### 第一優先：C# 手寫程式設計（最高占比）

非選擇題80%全部手寫，不能查文件，要熟到能直接寫出來：

- **OOP 四大柱**：封裝、繼承、多型、抽象
- **介面 vs 抽象類別**：差異、使用時機
- **泛型**：`class Stack<T> where T : class`
- **委派 / 事件**：`delegate`、`event`、`Action<T>`、`Func<T>`
- **LINQ**：`Where`、`Select`、`GroupBy`、`OrderBy`、`Join`、`FirstOrDefault`
- **async/await**：`Task`、`Task<T>`、`await`、`CancellationToken`
- **Exception Handling**：`try/catch/finally`、自訂 Exception
- **除錯題**：給一段有 bug 的 C# 程式碼，找出並修正

練習方式：**關掉 IDE，手寫在紙上**，模擬考試環境。

---

### 第二優先：MS SQL Server

工作有在用，重點補這幾個考試愛考的：

- **CTE**：`WITH cte AS (...) SELECT ...`
- **視窗函數**：`ROW_NUMBER() OVER (PARTITION BY ... ORDER BY ...)`、`RANK()`、`DENSE_RANK()`
- **JOIN 全類型**：INNER / LEFT / RIGHT / FULL OUTER
- **子查詢 vs EXISTS**
- **Stored Procedure、View、Trigger、Function** 的語法與差異
- **Transaction + ACID**：`BEGIN TRAN / COMMIT / ROLLBACK`
- **Index**：Clustered vs Non-Clustered、Covering Index
- **正規化**：1NF、2NF、3NF 的判斷與拆表

---

### 第三優先：ASP.NET Web Forms

- **Page 生命週期**順序：`PreInit` → `Init` → `Load` → 事件處理 → `PreRender` → `Render` → `Unload`
- **PostBack 機制**：IsPostBack 判斷
- **ViewState**：用途、儲存位置、如何停用
- **Session / Application / Cookie** 的差異與使用場景
- **Code-Behind 架構**：`.aspx` + `.aspx.cs`
- **Server Controls vs HTML Controls**
- **Web.config**：連線字串、AppSettings 設定方式

---

### 第四優先：資料結構與演算法

選擇題 + 可能的非選擇題：

- **Linked List**：手寫 Node 插入、刪除
- **Stack / Queue**：用 C# 陣列或 `List<T>` 實作
- **Binary Search Tree**：搜尋、插入、Inorder/Preorder/Postorder 走訪
- **排序演算法時間複雜度**：

| 演算法 | 最佳 | 平均 | 最壞 |
|--------|------|------|------|
| Bubble Sort | O(n) | O(n²) | O(n²) |
| Quick Sort | O(n log n) | O(n log n) | O(n²) |
| Merge Sort | O(n log n) | O(n log n) | O(n log n) |
| Binary Search | O(1) | O(log n) | O(log n) |

---

### 第五優先：系統分析與 UML

非選擇題可能出設計題，要能**手畫**：

- **Use Case Diagram**：Actor、用例、include/extend
- **Class Diagram**：繼承（實線空心箭頭）、介面實作（虛線空心箭頭）、關聯/聚合/組合
- **Sequence Diagram**：物件間的訊息呼叫順序
- **設計模式**：Repository、Factory、Singleton、MVC 各自的目的與結構

---

### 第六優先：HTML/JS/CSS（快速複習即可）

筆試不直接考，但報名資格要求，口試可能問：

- HTML5 語意標籤：`<header>`、`<nav>`、`<section>`、`<article>`、`<footer>`
- CSS Box Model、Flexbox 基本排版
- JavaScript DOM 操作、`fetch()` / AJAX 基本寫法

---

## 口試加分條件（準備說明）

| 條件 | 你的情況 |
|------|---------|
| 具系統架構設計經驗 | 可說明嘉晏的系統架構 |
| 有開發金融系統資歷 | **直接加分**，務必準備說明 |
| 具WinForm開發經驗 | 工作有用到，說明具體案例 |
| 具Bootstrap開發經驗 | 補充說明 |
| 具TFS或GIT版控技術 | 說明使用經驗 |
| 有微軟Blazor架構開發經驗（**尤佳**） | 若有餘力可稍微了解 |
| 具英語能力檢定證明（**尤佳**） | 如有證照記得帶 |

---

## 口試當天需攜帶文件（各3份）

1. 文件資料檢核表（至甄選專區下載）
2. 金融人才特質適性測驗結果（7/20前線上完成並列印）
3. 個人資料表含自傳（7/20前上傳）
4. 國籍具結書（至甄選專區下載）
5. 學位（畢業）證書影本
6. 勞保投保證明（勞保局e化服務系統下載）
7. 工作經歷說明表（親簽，詳述技能與工作內容）
8. 口試加分證明：專業證照、英語檢定等

---

## 建議複習時程

| 週次 | 主題 |
|------|------|
| 第1–2週 | C# OOP + LINQ + async/await 手寫練習 |
| 第3週 | SQL：CTE、視窗函數、Transaction、Index |
| 第4週 | ASP.NET Web Forms 生命週期 + 除錯題練習 |
| 第5週 | 資料結構手寫 + UML 手畫練習 |
| 第6週 | 模擬題 + 口試準備（工作內容說明） |
